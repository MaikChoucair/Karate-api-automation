plugins {
    id 'java'
}

group = 'com.maikol.api'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'com.intuit.karate:karate-junit5:1.4.1'
}

test {
    useJUnitPlatform()

    testLogging {
        events "passed", "failed", "skipped"
    }
}